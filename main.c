#include "lib/pmg.h"
#include "lib/funcs.h"

//Remover isso aqui depois.
/* #include <stdio.h>
#include <stdlib.h>
#include <time.h> */

int main(int argc,char **argv){
    //Apenas depuração.
/*     struct Image foto;
    foto.tipo = 5;
    foto.width = 10;
    foto.height = 10;
    foto.maxval = 255;
    foto.Data = (unsigned char **)calloc(foto.height,sizeof(unsigned char *));
    for(int i=0;i<foto.width;i++)foto.Data[i]=(unsigned char *)calloc(foto.height,sizeof(unsigned char));

    for(int i=0;i<foto.height;i++)for(int j=0;j<foto.width;j++)foto.Data[i][j]=rand()%100+1;

    printf("Imagem original:\n");
    viewPGMImage(&foto);

    foto = filtro(foto);

    printf("Imagem filtrada:\n");
    viewPGMImage(&foto);
 */
    return 0;
}